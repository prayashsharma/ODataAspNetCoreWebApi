{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "TodoDatabase": "Server=(localdb)\\mssqllocaldb;Database=OdataRestApiDb;Trusted_Connection=True;"
  },

  "ApiVersioningStyle": {
    //Http Header and Query String Versioning style is used by default. Header = x-api-version:1 or QueryString = ..api/Get?api-verion=1
    //Change value of UrlPathSegment to "true" if you prefer to use use url path segment like: ..api/v1/Get, ..api/v2/Put
    "UrlPathSegment": "false"
  }
}